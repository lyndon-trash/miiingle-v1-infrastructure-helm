apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-utils.fullname" . }}
  labels:
    {{- include "kube-utils.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-utils.selectorLabels" . | nindent 4 }}

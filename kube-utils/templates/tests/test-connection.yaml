apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kube-utils.fullname" . }}-test-connection"
  labels:
    {{- include "kube-utils.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kube-utils.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
